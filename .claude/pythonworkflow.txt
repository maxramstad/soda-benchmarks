Orchestrate the following workflow:

<Kernel>: symm
<Target>: Transformed
<Dimension>: MINI

If <Target> = Transformed:
1. Execute desired experiment using polybench-workflow agent
Important: Ensure that the linalg hls plan and affine hls plan are saved to the experiment_directory/transformation directory


if <Target> = Baseline
1. Execute desired experiment using polybench-workflow agent


if <Target> = Optimized
1. Execute desired experiment using polybench-workflow agent

On error: stop and report. Do not attempt fixes.
